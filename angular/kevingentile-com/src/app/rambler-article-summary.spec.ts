import { RamblerArticleSummary } from './rambler-article-summary';

describe('RamblerArticleSummary', () => {
  it('should create an instance', () => {
    expect(new RamblerArticleSummary()).toBeTruthy();
  });
});
